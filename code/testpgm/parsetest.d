program parstest1;

const
    a = 1;
    b = 1.1;
    c = 'abc';
    // f = a;
    // e = f;

var
    i : integer;
    p : array[2] of integer;

begin
    i := (i + 1) / 2 * 3 mod 4;
    // f();
    if a then
        i := a;
    elsif a then
        i := b;
    else
        i := i;
    end
end.
